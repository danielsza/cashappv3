Update the cash app to new version. complete rewrite, try and add client / server. better options for setup (com port) and proper password management.
 I lost the original source code for this. I have some info about it . The app currently use a unique code for each user. its currently hardcoded, but I would like to change that. it will do a basic calculation how much change to give a customer. and open a till. to open it it send writes com10 (but I forget which pin) high to drive a relay to open it. currently com10 is hard coded too, again we should change that. we also can open the till for change or for the start of the day  & end of the day. everything is logged to a text file, and in prior versions I had all errors. for example a wrong user code was entered, it sent a email and closed the app. currently we have 2 computers each with its own usb-com port and relay. I would like these to become the servers, with some sort of authentication between the client and server apps. the client app should talk to the server app. the server app should control the hardware. I would like to have 2 servers and the clients can connect to one, but if it fails then fall back to the other. also the server app should talk to the other server with its status and current IP / host name. originally this was written in VB, but it can be in a different language. it needs to work on windows 11. I'll include what I have of the current version. including a screenshot . IF THE APP ISNT WORKING CHECK FOR SERIAL DEVICES IN DEVICE MANAGER THAT MAY REQUIRE DRIVERS
WINDOWS UPDATE WILL FIND THE CORRECT ONES AUTOMATICALLY

WHEN THE SERIAL DEVICE IS INSTALLED PROPERLY IT MUST BE COM10 TO WORK.
A REBOOT HAS HISTORICALLY BEEN REQUIRED EVEN WITH WINDOWS 10 ONCE THE COM PORT IS CHANGED.


THE LOGS GET WRITTEN TO \\partsrv2\Parts\Cash\
 
UNAUTHORISED USER
9:29:07 AM
CONTROL:
INVOICE:
Total:
 IN:
Out:
Type:
